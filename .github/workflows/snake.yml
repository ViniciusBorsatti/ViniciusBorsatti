name: Generate Snake Animation

on:
  # Roda 1x por dia pra atualizar a cobrinha
  schedule:
    - cron: "0 0 * * *"
  # Permite rodar manualmente na aba Actions
  workflow_dispatch:
  # Roda sempre que der push na main
  push:
    branches:
      - main

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Gerar SVGs da snake
        uses: Platane/snk@v3
        id: snake-gif
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg?color_snake=%23e60000&color_dots=%23161b22,%23990000,%23cc0000,%23ff3333,%23ff6666
            dist/github-snake-dark.svg?palette=github-dark&color_snake=%23ff1a1a

      - name: Publicar na branch "output"
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
